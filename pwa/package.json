{
  "name": "hrm-fallen-tree-reporter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Offline-first PWA proof of concept for reporting fallen trees in Halifax Regional Municipality.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "icons": "node scripts/generate-icons.mjs",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@vitejs/plugin-react": "^5.0.4",
    "vite": "^7.1.14",
    "vite-plugin-pwa": "^1.0.3"
  }
}
